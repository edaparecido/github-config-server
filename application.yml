spring:
  application:
    name: aula-microservice
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
aula-microservice:
  greeting: "Seja"
  default-value: "bem vindo novamente!"